version: '3.8'

services:
  nome-servico:
    image: debian:latest
    container_name: nome-container
    stdin_open: true
    tty: true
    command: tail -f /dev/null
   
    restart: unless-stopped
    environment:
      - TZ=America/Sao_Paulo
      - USERID=1000
      - GROUPID=1000
    volumes:
      - samba-volume:/data

volumes:
  samba-volume:
