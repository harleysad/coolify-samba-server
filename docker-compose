version: '3.8'

services:
  webserver:
    image: nginx:alpine
    ports:
      - 2000:80
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
